# Настройка постоянного хранилища базы данных на Render.com

## Проблема

SQLite хранит данные в файле, но на Render.com файловая система **эфемерная** - при каждом деплое или перезапуске все файлы удаляются.

## Решение: PostgreSQL с Persistent Disk

### 1. Обновлен `render.yaml`

Добавлен PostgreSQL сервис с постоянным диском:

```yaml
# PostgreSQL Database (Persistent Storage)
- type: pserv
  name: gilamchi-db
  region: oregon
  plan: free
  env: docker
  disk:
    name: gilamchi-db-data
    mountPath: /var/lib/postgresql/data
    sizeGB: 1  # 1GB бесплатно
```

### 2. Backend подключен к PostgreSQL

```yaml
- key: DATABASE_URL
  fromDatabase:
    name: gilamchi-db
    property: connectionString
```

## Что это дает

✅ **Постоянное хранение** - данные НЕ удаляются при деплое
✅ **Автоматические бэкапы** - Render делает бэкапы
✅ **Масштабируемость** - PostgreSQL лучше для продакшена
✅ **Бесплатный план** - 1GB хранилища бесплатно

## Как задеплоить

### Вариант 1: Через Render Dashboard

1. Зайдите на https://render.com
2. Подключите GitHub репозиторий
3. Render автоматически обнаружит `render.yaml`
4. Нажмите "Apply" - создастся:
   - PostgreSQL база данных
   - Backend API
   - Frontend статический сайт

### Вариант 2: Через Git

```bash
git add render.yaml
git commit -m "Add PostgreSQL for persistent storage"
git push origin main
```

Render автоматически задеплоит изменения.

## Миграция данных (если уже есть SQLite данные)

Если у вас уже есть данные в SQLite и нужно перенести в PostgreSQL:

```bash
# 1. Экспорт из SQLite
sqlite3 carpet_shop.db .dump > data.sql

# 2. Подключиться к PostgreSQL на Render
# (получите connection string из Render Dashboard)

# 3. Импорт в PostgreSQL
psql $DATABASE_URL < data.sql
```

## Проверка

После деплоя проверьте:

1. **Database создана**: Render Dashboard → Databases → gilamchi-db
2. **Backend подключен**: Логи backend должны показать подключение к PostgreSQL
3. **Данные сохраняются**: Создайте тестовый товар, сделайте редеплой, проверьте что товар остался

## Важно

⚠️ **Бесплатный план Render**:
- PostgreSQL: 1GB хранилища
- Автоматически засыпает после 15 минут неактивности
- Первый запрос после сна может быть медленным (~30 сек)

💡 **Для продакшена**: рассмотрите платный план ($7/мес) для:
- Больше хранилища
- Без автоматического засыпания
- Лучшая производительность

## Альтернативные варианты

Если не хотите использовать Render PostgreSQL:

### Supabase (Рекомендуется)
- Бесплатно: 500MB PostgreSQL
- Не засыпает
- Дополнительные фичи (Auth, Storage, Realtime)

```yaml
- key: DATABASE_URL
  value: "postgresql://user:pass@db.supabase.co:5432/postgres"
```

### Neon.tech
- Бесплатно: 3GB PostgreSQL
- Serverless PostgreSQL
- Очень быстрый

### Railway.app
- Бесплатно: $5 кредитов/месяц
- Проще чем Render
- Автоматический PostgreSQL

## Текущая конфигурация

✅ `render.yaml` обновлен
✅ PostgreSQL настроен с persistent disk
✅ Backend автоматически подключится к PostgreSQL
✅ Готово к деплою!
